<Window x:Class="TeamBuddy.App.Views.TeamSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:TeamBuddy.App.Views"
        mc:Ignorable="d"
        DataContext="{StaticResource MainViewModel}"
        Title="My Profile" Height="720" Width="1024" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="Images/TB_wall1.jpg" Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>

        <views:TeamSelectionUserInfo Grid.Column="0" Visibility="Visible"/>
        <views:TeamSelectionTeamList Grid.Column="1"/>
        <views:CreateUserUC Grid.Column="1" Visibility="Collapsed"/>
    </Grid>
</Window>
